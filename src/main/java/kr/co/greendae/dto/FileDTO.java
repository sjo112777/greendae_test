package kr.co.greendae.dto;

import kr.co.greendae.entity.comment.Basic_Comment;
import kr.co.greendae.entity.file.Basic_File;
import kr.co.greendae.entity.file.Employ_File;
import kr.co.greendae.entity.file.State_File;
import lombok.*;

//@Data
//@AllArgsConstructor
//@NoArgsConstructor
//@ToString
//@Builder
public class FileDTO {
/*
    private int fno;
    private int ano;
    private String oName;
    private String sName;
    private int download ;
    private String rdate;

    // Entity 변환 메서드 정의
    public Basic_File toBasicFileEntity() {
        return Basic_File.builder()
                .fno(fno)
                .ano(ano)
                .oName(oName)
                .sName(sName)
                .download(download)
                .rdate(rdate)
                .build();
    }

    public Employ_File toEmployFileEntity() {
        return Employ_File.builder()
                .fno(fno)
                .ano(ano)
                .oName(oName)
                .sName(sName)
                .download(download)
                .rdate(rdate)
                .build();
    }

    public State_File toStateFileEntity() {
        return State_File.builder()
                .fno(fno)
                .ano(ano)
                .oName(oName)
                .sName(sName)
                .download(download)
                .rdate(rdate)
                .build();
    }
*/


}
