package kr.co.greendae.dto;

import kr.co.greendae.entity.article.Basic_Article;
import kr.co.greendae.entity.article.Employ_Article;
import kr.co.greendae.entity.article.State_Article;
import kr.co.greendae.entity.comment.Basic_Comment;
import kr.co.greendae.entity.comment.Employ_Comment;
import kr.co.greendae.entity.comment.State_Comment;
import lombok.*;

//@Data
//@AllArgsConstructor
//@NoArgsConstructor
//@ToString
//@Builder
public class CommentDTO {

    /*
    private int cno;
    private int parent;
    private String content;
    private String writer;
    private String regip;
    private String wdate;


    // Entity 변환 메서드 정의
    public Basic_Comment toBasicCommentEntity() {
        return Basic_Comment.builder()
                .cno(cno)
                .parent(parent)
                .content(content)
                .writer(writer)
                .regip(regip)
                .wdate(wdate)
                .build();
    }

    public Employ_Comment toEmployCommentEntity() {
        return Employ_Comment.builder()
                .cno(cno)
                .parent(parent)
                .content(content)
                .writer(writer)
                .regip(regip)
                .wdate(wdate)
                .build();
    }

    public State_Comment toStateCommentEntity() {
        return State_Comment.builder()
                .cno(cno)
                .parent(parent)
                .content(content)
                .writer(writer)
                .regip(regip)
                .wdate(wdate)
                .build();
    }

     */

}
